@model IEnumerable<ThAmCo.Events.Models.GuestBookingViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Guest)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Guest.Forename)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event.EventId)
            </td>
            <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.EventId, id2=item.GuestId }) |
                    @Html.ActionLink("Details", "Details", new { id=item.EventId, id2=item.GuestId }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.EventId, id2=item.GuestId })
            </td>
        </tr>
}
    </tbody>
</table>
